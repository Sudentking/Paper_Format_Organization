# 文档预览功能开发计划

## 已完成的优化修改（v0.3.x）

以下三项优化已在之前的迭代中完成并推送：

### 1. 目录页码右对齐（toc_formatter.py）
- **问题**：目录页码没有对齐右侧，标题和页码位置错乱
- **方案**：为 SDT 目录条目添加 `w:tabs > w:tab` 右对齐制表位 + 点号前导符（`w:leader="dot"`, `w:pos="8296"`），在页码前插入 Tab 字符
- 为段落模式目录条目添加 `Cm(14.65)` 右对齐制表位 + 点号前导符
- 添加 `TRAILING_NUM_PATTERN` 正则匹配末尾页码，与标题部分分离

### 2. 前端排版优化（index.html）
- **问题**：标题格式设置区域布局混乱，所有级别挤在一起
- **方案**：添加 `.config-row` / `.config-row-label` CSS 类实现水平行布局
- 每个标题级别独占一行（一级标题/二级标题/三级标题各一行）
- 每行包含：字体、字号、对齐、行距 四个下拉选择器

### 3. 字号范围扩展（index.html）
- **问题**：字号选项不够丰富
- **方案**：标题字号增加到 9 个选项（小五 9pt ~ 一号 26pt）
- 正文字号增加到 6 个选项（小五 9pt ~ 三号 16pt）
- 表格/图标题字号增加到 4 个选项
- 目录字号增加到 5 个选项
- 行距增加到 7 个选项（1.0 ~ 3.0）

---

## 已完成功能：文档预览

### 实现状态：✅ 已完成

### 技术方案：python-docx 直接渲染（已替代 mammoth）

**核心思路**：服务端使用 python-docx 直接遍历文档 XML，将段落/表格/SDT 转换为 HTML，前端通过 iframe 展示。

**为什么从 mammoth 切换到 python-docx**：
- mammoth 在解析格式化后的 SDT TOC 内容时会崩溃（`pop from empty list`）
- 原因：我们的 TOC 格式化器重写了 SDT 中的 runs，破坏了 `w:fldChar` 域标记序列
- python-docx 直接读取文档结构，不依赖域标记，更加健壮
- 无需额外依赖（mammoth 已从 requirements.txt 移除）

**标题检测方案**（`_detect_heading_level`）：
- 优先检查 `w:outlineLvl` XML 属性（我们的格式化器设置的）
- 其次检查 Word 样式名称（Heading 1/2/3）
- 最后检查中文样式名（标题 1/2/3）

**HTML 渲染限制与对策**：
- **字体差异**：注入 CSS 指定 `font-family`（宋体/黑体/Times New Roman）
- **页面布局**：CSS 模拟 A4 宽度 210mm，居中显示
- **表格样式**：python-docx 读取单元格文本，CSS 设置边框
- **SDT 内容**：专门处理 `w:sdt > w:sdtContent` 中的段落和表格

### 已修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `requirements.txt` | 修改 | 移除 mammoth（不再需要） |
| `services/api/app.py` | 修改 | 添加 `/api/preview/{session_id}/{filename}` 端点 |
| `services/api/frontend/index.html` | 修改 | 添加预览页面（三步流程改造） |
| `services/doc_preview.py` | 新建 | python-docx 文档转 HTML 预览模块 |
| `tests/test_preview.py` | 新建 | 预览功能测试（7 个） |

### 用户交互流程

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 上传文档 | 拖拽或点击上传 .docx 文件 |
| 2 | 调整格式 | 选择标题/正文/表格/目录等格式设置 |
| 3 | 点击"确认修改" | 开始格式化，显示进度条 |
| 4 | 自动跳转预览页 | 预览页展示格式化后的 HTML 渲染效果 |
| 5 | 确认下载 | 点击"确认下载文档"下载 .docx 文件 |

### 预览页 UI

```
┌─────────────────────────────────────────────┐
│  ← 返回修改    文档预览    📥 确认下载文档   │  ← 顶部工具栏
├─────────────────────────────────────────────┤
│  ⚠️ 预览为 HTML 渲染效果，可能与 Word 有差异  │  ← 兼容性提示
├─────────────────────────────────────────────┤
│  ┌─────────────────────────────────────┐    │
│  │        格式化后的文档内容             │    │  ← A4 纸张模拟
│  │        (HTML 渲染, iframe)          │    │
│  └─────────────────────────────────────┘    │
├─────────────────────────────────────────────┤
│     [← 返回修改设置]    [确认下载文档 ↓]     │  ← 底部操作栏
└─────────────────────────────────────────────┘
```

### 测试结果
- 41 个测试全部通过（原有 34 个 + 新增 7 个预览测试）
- 用真实论文文档验证：9 个 h1、19 个 h2、28 个 h3、11 个表格正确渲染

---

## 当前待办：提交并推送修复

### 状态：代码已修改完成，待提交推送

已完成的代码改动：
1. `services/doc_preview.py` — 从 mammoth 切换到 python-docx 直接渲染
2. `requirements.txt` — 移除 mammoth 依赖
3. `tests/test_preview.py` — 测试适配新实现
4. `preview_plan.md` — 更新计划文档

待执行操作：
- `git add -A && git commit && git push origin master`
