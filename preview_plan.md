# 文档预览功能开发计划

## 已完成的优化修改（v0.3.x）

以下三项优化已在之前的迭代中完成并推送：

### 1. 目录页码右对齐（toc_formatter.py）
- **问题**：目录页码没有对齐右侧，标题和页码位置错乱
- **方案**：为 SDT 目录条目添加 `w:tabs > w:tab` 右对齐制表位 + 点号前导符（`w:leader="dot"`, `w:pos="8296"`），在页码前插入 Tab 字符
- 为段落模式目录条目添加 `Cm(14.65)` 右对齐制表位 + 点号前导符
- 添加 `TRAILING_NUM_PATTERN` 正则匹配末尾页码，与标题部分分离

### 2. 前端排版优化（index.html）
- **问题**：标题格式设置区域布局混乱，所有级别挤在一起
- **方案**：添加 `.config-row` / `.config-row-label` CSS 类实现水平行布局
- 每个标题级别独占一行（一级标题/二级标题/三级标题各一行）
- 每行包含：字体、字号、对齐、行距 四个下拉选择器

### 3. 字号范围扩展（index.html）
- **问题**：字号选项不够丰富
- **方案**：标题字号增加到 9 个选项（小五 9pt ~ 一号 26pt）
- 正文字号增加到 6 个选项（小五 9pt ~ 三号 16pt）
- 表格/图标题字号增加到 4 个选项
- 目录字号增加到 5 个选项
- 行距增加到 7 个选项（1.0 ~ 3.0）

---

## 新功能：文档预览

## 目标
用户上传文档并选择格式设置后，点击"确认修改"按钮，跳转到结果预览页。预览页展示格式化后的文档内容，用户确认无误后点击"下载"按钮下载最终文档。

## 技术方案

### 预览渲染方案：Python mammoth 库（服务端 HTML 转换）

**核心思路**：服务端使用 `mammoth` 库将格式化后的 `.docx` 转换为 HTML，前端通过 iframe 展示。

**选择理由**：
- mammoth 是纯 Python 库，轻量无外部依赖，与现有 FastAPI 后端无缝集成
- 项目技术规划（agent.md）中已选定此方案
- API 极简：`mammoth.convert_to_html(file)` 一行完成转换
- 输出的 HTML 天然适合网页展示，支持内嵌图片（base64）

**HTML 渲染限制与对策**：
- **字体差异**：HTML 中的字体取决于用户系统安装了哪些字体 → 在转换的 HTML 中注入 CSS 指定 `font-family`，优先使用文档中设置的中文字体
- **页面布局**：HTML 不支持分页、页边距 → 添加 CSS `@media print` 模拟 A4 页面宽度，添加分页虚线提示
- **表格样式**：复杂合并单元格可能显示差异 → 保留 mammoth 默认表格渲染，必要时通过 style map 微调
- **目录超链接**：TOC 在 HTML 中变为可点击链接 → 这是正面效果，提升用户体验

## 修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `requirements.txt` | 修改 | 添加 `mammoth` 依赖 |
| `services/api/app.py` | 修改 | 添加 `/api/preview/{session_id}/{filename}` 端点 |
| `services/api/frontend/index.html` | 修改 | 添加预览页面（三步流程改造） |
| `services/doc_preview.py` | 新建 | 文档转 HTML 预览模块 |
| `tests/test_preview.py` | 新建 | 预览功能测试 |

## 实现步骤

### 步骤 1：安装 mammoth 并创建预览转换模块
- `pip install mammoth`
- 更新 `requirements.txt`
- 新建 `services/doc_preview.py`：
  - `convert_docx_to_html(docx_path: str) -> str` 函数
  - 使用 `mammoth.convert_to_html()` 转换
  - 注入自定义 CSS 样式（字体、页面宽度、表格边框等）
  - 包装为完整 HTML 文档（含 `<html>`, `<head>`, `<body>`）
  - 处理图片：mammoth 默认内联 base64，无需额外处理

### 步骤 2：添加预览 API 端点
- 在 `app.py` 中添加 `GET /api/preview/{session_id}/{filename}` 端点
- 查找 session 目录中的格式化后 .docx 文件
- 调用 `convert_docx_to_html()` 返回完整 HTML
- 返回 `HTMLResponse` 而非 JSON

### 步骤 3：前端改造 — 三步流程
改造 `index.html` 的用户交互流程：

**现有流程**：上传 → 设置 → 点击格式化 → 进度条 → 下载按钮
**新流程**：上传 → 设置 → 点击"确认修改" → 跳转预览页 → 确认下载

具体改动：
1. 将"开始格式化"按钮改为"确认修改"
2. 格式化完成后，不再显示下载按钮，而是自动跳转到预览页
3. 新建预览页面（可以在同一个 HTML 中用 JS 切换视图）：
   - 顶部工具栏：返回按钮、文档标题、下载按钮
   - 主体区域：iframe 加载预览 HTML
   - 底部操作栏：确认下载按钮
4. 预览页面样式：模拟文档纸张效果（白色背景、阴影、A4 宽度居中）

### 步骤 4：编写测试
- 测试 `convert_docx_to_html()` 返回完整 HTML
- 测试 HTML 包含正确的 CSS 样式
- 测试预览 API 端点返回 200 状态码
- 测试不存在的文件返回 404

### 步骤 5：测试验证并推送
- 运行全部单元测试（原有 34 个 + 新增预览测试）
- 用真实文档测试端到端流程
- 提交并推送到 GitHub

## 预览页 UI 设计

```
┌─────────────────────────────────────────────┐
│  ← 返回修改    文档预览    📥 下载格式化文档  │  ← 顶部工具栏
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────────────────────────────┐    │
│  │                                     │    │  ← A4 纸张模拟
│  │        格式化后的文档内容             │    │
│  │        (HTML 渲染)                  │    │
│  │                                     │    │
│  │                                     │    │
│  └─────────────────────────────────────┘    │
│                                             │
├─────────────────────────────────────────────┤
│     [← 返回修改设置]    [确认下载文档 ↓]     │  ← 底部操作栏
└─────────────────────────────────────────────┘
```

## 风险与注意事项
1. mammoth 转换的 HTML 可能无法完美还原 Word 排版，需在 UI 上添加提示文字
2. 大文档（>50MB）转换可能较慢，需要在 API 中添加超时处理
3. 预览 HTML 中的图片以 base64 内联，可能导致 HTML 较大，需注意内存使用
